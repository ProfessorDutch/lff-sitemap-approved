export const loadingSpinnerStyles = {
  wrapper: "min-h-screen flex items-center justify-center bg-gray-50",
  spinner: "w-16 h-16 border-4 border-[--primary]/20 border-t-[--primary] rounded-full animate-spin"
} as const;